def format_name(f_name, l_name):
    """Take first and last name and format it to return 
    the title case version of the name."""
    name = f"{f_name.title()} {l_name.title()}"
    return name

print(format_name("kritika", "TAAnk"))
format_name(f_name, l_name)
